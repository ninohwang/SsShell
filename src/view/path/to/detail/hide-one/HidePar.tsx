import React, {useEffect} from 'react'

interface HideParProps {
    Style?: React.CSSProperties,
}

export default (props: HideParProps): React.FC => {
    useEffect(() => {

    }, [])

    return (

    )
}
