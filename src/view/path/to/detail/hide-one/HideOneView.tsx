import React, {useEffect} from 'react'

interface HideOneProps {
    Style?: React.CSSProperties,
}

const HideOneView: React.FC<HideOneProps> = props => {
    useEffect(() => {

    }, [])

    return (
        
    )
}

export default HideOneView
